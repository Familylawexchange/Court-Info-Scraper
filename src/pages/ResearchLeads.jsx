(async function(){const app=document.getElementById('app');const leads=await fetch('/api/admin/research-leads').then(r=>r.json());app.innerHTML=`<form id="f"><label>Source platform</label><select name="source_name"><option>Trellis Law</option><option>Westlaw</option><option>Lexis</option><option>UniCourt</option><option>Docket Alarm</option><option>vLex/Fastcase</option><option>PACER</option><option>Official court site</option><option>News/public source</option><option>Other</option></select><input name="source_url" placeholder="source URL"><input name="case_name" placeholder="case name"><input name="case_number" placeholder="case number"><input name="state" placeholder="state"><input name="county" placeholder="county"><input name="court" placeholder="court"><input name="person_name" placeholder="person name"><input name="role" placeholder="role"><input name="judge" placeholder="judge"><input name="guardian_ad_litem" placeholder="GAL"><input name="attorneys" placeholder="attorneys"><input name="prosecutor" placeholder="prosecutor"><input name="evaluator" placeholder="evaluator"><input name="document_title" placeholder="document title"><textarea name="docket_entry_text" placeholder="docket entry text"></textarea><input type="date" min="1990-01-01" name="filing_date"><textarea name="notes" placeholder="notes/tags"></textarea><input name="verification_source" placeholder="official verification source"><input type="file" name="file"><button>Create lead</button></form><pre id="out"></pre><h2>Leads</h2><table><tr><th>ID</th><th>Source</th><th>Case</th><th>Status</th><th>Action</th></tr>${leads.map(l=>`<tr><td>${l.id}</td><td>${l.source_name}</td><td>${l.case_name||''}</td><td>${l.status}</td><td><button onclick="convert(${l.id})">Convert to raw result</button></td></tr>`)}</table>`;document.getElementById('f').onsubmit=async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const file=e.target.file.files[0];if(file){body.file_name=file.name;body.file_type=file.type;body.file_base64=await new Promise(ok=>{const reader=new FileReader();reader.onload=()=>ok(reader.result);reader.readAsDataURL(file);});}const r=await fetch('/api/admin/research-leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});document.getElementById('out').textContent=JSON.stringify(await r.json(),null,2);};window.convert=async id=>{await fetch(`/api/admin/research-leads/${id}/convert/raw-result`,{method:'POST'});location.reload();};})();
