(async function(){
 const app=document.getElementById('app');
 const [sources,groups,jobs]=await Promise.all([fetch('/api/sources').then(r=>r.json()),fetch('/api/keyword-groups').then(r=>r.json()),fetch('/api/admin/scanner-jobs').then(r=>r.json())]);
 app.innerHTML=`<h2>Run Scanner Job</h2><form id="f"><label>Source</label><select name="source_id">${sources.map(s=>`<option value="${s.id}">${s.name}</option>`)}</select><label>State</label><input name="state"><label>County</label><input name="county"><label>Court</label><input name="court"><label>Keyword Group</label><select name="keyword_group">${groups.map(g=>`<option>${g.name}</option>`)}</select><label>Custom keywords (comma-separated)</label><input name="custom_keywords"><label>Person/entity name</label><input name="person_name"><label>Role</label><input name="role"><label>Case type</label><input name="case_type"><label>Date start (1990 or later)</label><input name="date_start" type="date" min="1990-01-01"><label>Date end</label><input name="date_end" type="date"><label>Max results</label><input name="max_results" type="number" value="25"><button>Run job</button></form><pre id="out"></pre><h2>Recent Jobs</h2><table><tr><th>ID</th><th>Source</th><th>Status</th><th>Results</th><th>Error</th></tr>${jobs.map(j=>`<tr><td>${j.id}</td><td>${j.source_name||''}</td><td>${j.status}</td><td>${j.results_count}</td><td>${j.error_message||''}</td></tr>`).join('')}</table>`;
 document.getElementById('f').onsubmit=async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch('/api/admin/scanner-jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});document.getElementById('out').textContent=JSON.stringify(await r.json(),null,2);};
})();
