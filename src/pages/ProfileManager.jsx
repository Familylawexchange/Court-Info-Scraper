(async function(){const profiles=await fetch('/api/admin/profiles').then(r=>r.json());document.getElementById('app').innerHTML=`<form id="f"><input name="name" placeholder="name" required><input name="role" placeholder="role"><input name="court_or_office" placeholder="court/office"><input name="firm_or_agency" placeholder="firm/agency"><input name="county" placeholder="county"><input name="state" placeholder="state"><input name="bar_number" placeholder="bar number"><textarea name="known_cases" placeholder="known cases"></textarea><button>Create profile</button></form><pre id="out"></pre><h2>Profiles</h2>${profiles.map(p=>`<article><h3>${p.name}</h3><p>${p.role||''} · ${p.county||''} ${p.state||''} · ${p.visibility}</p><button onclick="summary(${p.id})">Generate source-bound summary</button></article>`).join('')}`;document.getElementById('f').onsubmit=async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch('/api/admin/profiles',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});document.getElementById('out').textContent=JSON.stringify(await r.json(),null,2);};window.summary=async id=>{document.getElementById('out').textContent=JSON.stringify(await fetch(`/api/admin/profiles/${id}/summary`).then(r=>r.json()),null,2);};})();
