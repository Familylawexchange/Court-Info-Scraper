(async function(){const id=location.pathname.split('/').pop();const data=await fetch(`/api/profile/${id}`).then(r=>r.json());const sections=data.sections||{};document.getElementById('app').innerHTML=`<h2>${data.profile?.name||'Profile unavailable'}</h2><p>${data.profile?.role||''} · ${data.profile?.court_or_office||data.profile?.firm_or_agency||''} · ${data.profile?.county||''} ${data.profile?.state||''}</p><p>Profile completeness: ${data.profile?.profile_status||'new profile'}</p><p>${data.warning||''}</p>${Object.entries(sections).map(([k,v])=>`<h3>${k}</h3>${v.length?`<ul>${v.map(x=>`<li>${x}</li>`).join('')}</ul>`:'<p>No approved public records in this category.</p>'}`).join('')}`;})();
